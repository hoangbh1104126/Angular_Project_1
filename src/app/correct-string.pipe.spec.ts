import { CorrectStringPipe } from './correct-string.pipe';

describe('CorrectStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectStringPipe();
    expect(pipe).toBeTruthy();
  });
});
