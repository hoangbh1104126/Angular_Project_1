import { HighlightSearchPipe } from './highlight-search.pipe';

describe('HighlightSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
